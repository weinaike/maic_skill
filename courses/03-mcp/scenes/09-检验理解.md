---
type: quiz
title: 检验理解
---

## 讲稿

内容讲完了，三道题检验一下。前两道选择题，概念辨析和场景判断；第三道是动手题——把你自己的函数拿出来，现场定接口。做完对照解析，看看哪一节需要回去翻一遍。

## 题目

```quiz
{
  "questions": [
    {
      "id": "q1",
      "type": "single",
      "question": "你要让 Agent 按关键词检索实验数据库并把结果拿回来处理，最合适的做法是？",
      "options": [
        { "label": "做成 @mcp.tool，参数带关键词与条数，返回结构化结果", "value": "A" },
        { "label": "做成 @mcp.resource，把整张表按 URI 暴露出去", "value": "B" },
        { "label": "做成 @mcp.prompt，让用户每次手写查询语句", "value": "C" }
      ],
      "answer": ["A"],
      "analysis": "检索是一个要执行的动作、要拿回结果，属于 tool 的职责。resource 适合暴露只读数据供读取，prompt 是固化提问模板，都不对题。",
      "hasAnswer": true,
      "points": 1
    },
    {
      "id": "q2",
      "type": "single",
      "question": "给参数标注 sort: Literal[\"date\", \"relevance\"]，最主要的收益是？",
      "options": [
        { "label": "代码更短，运行更快", "value": "A" },
        { "label": "取值被约束成枚举，模型传参不会跑偏", "value": "B" },
        { "label": "用户可以不传任何参数", "value": "C" }
      ],
      "answer": ["B"],
      "analysis": "Literal 会被转成 schema 的枚举约束，模型只能从合法取值里选，直接堵死“传错值”这类高频错误；与运行速度无关。",
      "hasAnswer": true,
      "points": 1
    },
    {
      "id": "q3",
      "type": "short_answer",
      "question": "挑一个你自己的科研函数，写出：① 工具名；② 两个参数（名称+类型+必选/可选）；③ 一句工具描述（写给模型看的）。对照 §5 与 §7 的要点自查。",
      "answer": [],
      "hasAnswer": false,
      "commentPrompt": "从三方面评：类型是否具体（不用裸参数）；必选/可选是否合理；描述是否讲清“何时用/怎么传/何时不用”。",
      "points": 2
    }
  ]
}
```
