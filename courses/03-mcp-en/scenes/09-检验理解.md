---
type: quiz
title: Check Your Understanding
---

## 讲稿

That's all the content — time to test it with three questions. The first two are multiple choice: one on telling concepts apart, one on judging a scenario. The third is hands-on: take one of your own functions and define its interface on the spot. When you're done, check the explanations and see which section is worth another read.

## 题目

```quiz
{
  "questions": [
    {
      "id": "q1",
      "type": "single",
      "question": "You want your Agent to search an experiment database by keyword and bring the results back for processing. Which approach fits best?",
      "options": [
        { "label": "Make it an @mcp.tool with keyword and count parameters, returning structured results", "value": "A" },
        { "label": "Make it an @mcp.resource exposing the whole table by URI", "value": "B" },
        { "label": "Make it an @mcp.prompt and have the user hand-write the query every time", "value": "C" }
      ],
      "answer": ["A"],
      "analysis": "Searching is an action you execute that brings back results — that's a tool's job. A resource fits exposing read-only data for lookup, and a prompt freezes a question template. Neither matches here.",
      "hasAnswer": true,
      "points": 1
    },
    {
      "id": "q2",
      "type": "single",
      "question": "What is the main benefit of annotating a parameter as sort: Literal[\"date\", \"relevance\"]?",
      "options": [
        { "label": "Shorter code that runs faster", "value": "A" },
        { "label": "Values are constrained to an enum, so the model can't pass bad arguments", "value": "B" },
        { "label": "The user can skip the parameter entirely", "value": "C" }
      ],
      "answer": ["B"],
      "analysis": "Literal becomes an enum constraint in the schema, so the model can only choose from legal values. That shuts down the high-frequency error of passing an illegal value outright. It has nothing to do with runtime speed.",
      "hasAnswer": true,
      "points": 1
    },
    {
      "id": "q3",
      "type": "short_answer",
      "question": "Pick one of your own research functions and write out: 1) a tool name; 2) two parameters (name + type + required/optional); 3) a one-sentence tool description, written for the model. Check your work against the key points in §5 and §7.",
      "answer": [],
      "hasAnswer": false,
      "commentPrompt": "Grade on three fronts: are the types concrete (no bare parameters); is required/optional sensible; does the description make clear when to use the tool, how to pass arguments, and when not to use it.",
      "points": 2
    }
  ]
}
```
